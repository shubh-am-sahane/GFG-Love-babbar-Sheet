 {
        //code here
       set<int> s;
       for(int i=0;i<n;i++){
           s.insert(a[i]);
       }
       for(int i=0;i<m;i++){
           s.insert(b[i]);
       }
       
       return s.size();
       // for returning the elements in union
       for (auto itr = s.begin(); itr != s.end(); itr++)
        cout << *itr
             << " ";
                     
}
   
    }
